@extends('admin-pages.layout')

@section('before-admin-page-content')
    @include('admin-pages.{{ path }}.navigation')
@endsection

@section('content')
    @if(isset($requestParams['tab']) && $requestParams['tab'] == 'tab-1')
        @include('admin-pages.{{ path }}.tab-1')
    @else
        @include('admin-pages.{{ path }}.dashboard')
    @endif
@endsection
@extends('user-meta-boxes.layout')

@section('after-meta-box-header')
    @include('user-meta-boxes.{{ id }}.navigation')
@endsection

@section('content')
    @if(isset($requestParams['{{ id }}-tab']) && $requestParams['{{ id }}-tab'] == 'tab-2')
        @include('user-meta-boxes.{{ id }}.tab-2')
    @else
        @include('user-meta-boxes.{{ id }}.tab-1')
    @endif
@endsection
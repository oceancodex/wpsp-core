@extends('modules.user-meta-boxes.layout')

@section('after-meta-box-header')
    @include('modules.user-meta-boxes.{{ id }}.navigation')
@endsection

@section('content')
    @if(isset($requestParams['{{ id }}-tab']) && $requestParams['{{ id }}-tab'] == 'tab-2')
        @include('modules.user-meta-boxes.{{ id }}.tab-2')
    @else
        @include('modules.user-meta-boxes.{{ id }}.tab-1')
    @endif
@endsection